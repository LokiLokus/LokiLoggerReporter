killold:
  stage: clean
  script:
   - if pgrep dotnet; then pkill dotnet; fi

runProject:
  stage: run
  script:
    - ls
    - rm appsettings.json
    - mv /appsettings.json appsettings.json
    - dotnet run

stages:
  - clean
  - run
